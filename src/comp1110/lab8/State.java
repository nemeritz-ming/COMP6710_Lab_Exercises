package comp1110.lab8;

import javafx.scene.paint.Color;

public enum State {
    ACTIVE, INACTIVE;

    /*
     * Given a State s, returns the 'alive' color if the state is ACTIVE
     * and the 'dead' colour if the state is INACTIVE.
     */
    static Color colorFromState(State s) {
        // TODO 1 Fix me
        return null;
    }
}
